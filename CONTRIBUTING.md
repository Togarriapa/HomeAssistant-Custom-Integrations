# Adding an integration to the collection

Every listed integration must:

- Be hosted in a separate public GitHub repository.
- Contain exactly one integration directory under `custom_components/`.
- Include a versioned `manifest.json` with documentation and issue-tracker links.
- Include `hacs.json`.
- Pass the HACS validation action.
- Be added to both `repositories.json` and the README table.
